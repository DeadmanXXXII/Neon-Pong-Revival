name: Build Flutter Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-apk:
    name: 🧱 Build DeadmanXXXIIs Classic Pong APK
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      - name: 🧰 Install System Dependencies
        run: |
          sudo apt-get update -y && sudo apt-get upgrade -y
          sudo apt-get install -y curl git unzip xz-utils zip libglu1-mesa

      - name: 🐦 Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.13.0'
          channel: 'stable'

      - name: 🛠️ Initialize Project Structure (The Fix)
        # CRITICAL FIX: Explicitly set the project name to the valid snake_case name
        # 'deadman_pong_flutter' to bypass the "is not a valid Dart package name" error
        # caused by the repository name 'Neon-Pong-Revival'.
        run: flutter create --project-name neon_pong_revival .

      - name: 📦 Install Dart & Flutter Packages
        run: flutter pub get

      - name: 🧱 Build Release APK
        run: flutter build apk --release

      - name: 📤 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: DeadmanXXXII_ClassicPong_Android
          path: build/app/outputs/flutter-apk/app-release.apk
